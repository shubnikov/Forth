/*  4th list 
        Complete or incomplete definitions
        name,link,type,@proc,proc_name
        name,link,type,@call,&addr1,...,&exit
*/
/*  X4th list 
        name,link,type,@proc
    S" 
*/ 
void compile() {
    //  do {
    // 	    get word from the input;
    // 	    if (word is empty)
    //		    break;
    //      find it among C++ definitions
    // 
}
